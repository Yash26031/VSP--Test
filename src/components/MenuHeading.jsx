/** @format */

import React from "react";

const MenuHeading = ({ title }) => {
  return (
    <div className="uppercase text-sm font-semibold px-4 py-4">{title}</div>
  );
};

export default MenuHeading;
